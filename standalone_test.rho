// Standalone test for while loop
counter = 0
sum = 0

// Basic while loop
while (counter < 5) {
    sum = sum + counter
    counter = counter + 1
}

// Verify result
assert(sum == 10)
EOF < /dev/null